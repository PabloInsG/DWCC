/*
let usuario;

while(usuario != "g"){
    let usuario = prompt(`a- Crear un conxunto vacío.
b- Eliminar un conxunto.
c- Crear un conxunto coa unión de dous conxuntos. Unir os dous en un.
d- Crear un conxunto ca intersección de dous conxuntos (só os elementos que están nos dous conxuntos).
e- Introducir un elemento no conxunto indicado.
f- Eliminar un elemento do conxunto indicado.
g- Saír.`)

switch(usuario){
    case "a":
        let conjuntoUsuario = prompt(`Indicar o conxunto a crear. Por exemplo: libros.`);
        var conjuntos = {};
        conjuntos[conjuntoUsuario] = new Set();
        console.log(conjuntos);
    case "b":
        let eliminarConjunto = prompt(`Indicar o conxunto a eliminar. Por exemplo: libros.`);
        conjuntos.delete(eliminarConjunto);
    case "c":
    
    case "d":

    case "e":

    case "f":

    case "g":
        return;
}
}
*/